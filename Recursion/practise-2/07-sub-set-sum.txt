In this problem we need to return all the subsets whose sum is 
equal to the given target

This is what i am guessing:
Subsets we obtain with include and exclude, everytime a subset is added
we need to add the sum the the result variable

Anytime sum reaches target we need to add all the subset values 
to the result and return 

Suppose i am adding 3 to slate then target= target-3 
that i send to my subordinate 
my subordinate first check target ==0 then it adds the slate to
the result 

program is:

if target == 0:
  result.append(slate[:])
  return 
helper(slate,i+1,target)
slate.append(nums[i])
helper(slate,i+1,target-nums[i])
slate.pop()

Lets see 1,2,3,4  target =6 

0:1  ([],1,6) -> 1:1 ([],2,6) -> 2:1 ([],3,6) -> 3:1 ([],4,6) -> 
4:1 ([],5,6) ->5:1 ([],6,6) This has to return otherwise goes into 
infinite loop 
hence modify program 
if i == len(num):
  return
if target == 0:
  result.append(slate[:])
  return 
helper(slate,i+1,target)
slate.append(nums[i])
helper(slate,i+1,target-nums[i])
slate.pop()
Now the program returns:
[1,2,3,4]
0:1  ([],1,6) -> 1:1 ([],2,6) -> 2:1 ([],3,6) -> 3:1 ([],4,6) -> returns 
Now 3:2 -> ([4],4,6-4 =2)-> returns because target not 0 
2:2 ([3],3,6-3=3) -> 3:1 ([3],4,3)-> returns
                  -> 3:2 ([3,4],4,-1) -> returns 
2:2 returns 
1:2 ([2],2,6-2=4) -> 2:1 ([2],3,4) -> 3:1 ([2],4,4) -> returns 
3:2 ([2,4],4,0) -> returns result [2,4]
2:2  ([2,3],3,1) -> 3:1 ([2,3],4,1) -> returns 
3:2 -> ([2,3,4],4,-3) -> returns 
now 1:2 returns 
now 0:2 (1,1,5) -> 1:1 (1,2,5) -> 2:1 (1,3,5) -> 3:1 (1,4,5) -> returns 

3:2 -> ([1,4],4,1) -> returns 

2:2 ([1,3],3,2) -> 3:1 ([1,3],4,2) -> returns
3:2 [(1,3,4),4,-2] -> returns 

1:2 ([1,2],2,3) -> 2:1 ([1,2],3,3) -> 3:1 ([1,2],4,3) -> returns 
3:2 ([1,2,4],4,-1) -> returns 
2:2 ([1,2,3],3,0) -> returns by adding result [1,2,3] 
now everything returns back from 2:2 returns and 1:2 returns and
0:2 returns 

Only change need to make here is:
First we need to check the target ==0 and then we need to check i==n 
Otherwise in cases where i==n and target ==0 result is not added to 
the slate 

Hence the solution is:

if target == 0:
  result.append(slate[:])
  return 

if i == len(num):
  return

helper(slate,i+1,target)
slate.append(nums[i])
helper(slate,i+1,target-nums[i])
slate.pop()


