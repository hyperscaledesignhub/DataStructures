what is combinations?
Combination means choosing k items from n given 
items of the input
How many ways we can chose k items from the n input items
C(n,k)
Here order doesn't matter
Where as permutation which is arrangements
of items order matters because if i arrange 
an item in 1,2 or 2,1 both are different 

But in combinations since we are selecting items it doesn't matter How
we are selecting the items, either i can select 1 first 2 second 
or 2 first 1 second doesn't matter 
Suppose i want to choose 2 items from the given input of items 
A,B,C,D and E 
Combination are AB AC AD AE BC BD BE CD CE,DE 
C(5,2) = 5!/3!2! = 10 
That means we have to take a slate of length=2 
and we can start arranging the items 

For pos=1 of A, pos=2 keep B C D E 
For pos=1 of B, pos=2 keep C D E 
For pos=1 of C, pos=2 keep D E 
for pos=1 of D, pos=2 keep E 
pos=1 from 0 to len-1(exclude len-1 element)
pos=2 from start from pos=1 of the input ABCDE, from B to E,
that means from pos=i+1 to len(input) 
That means my recursive function should look like this
#Base case
if i == combination_size:
  result.append(slate[:])
  return 


for k in range(i,len(nums)):
    slate.append(num[k])
    helper(slate,i+1)
    slate.pop()
For slot-2 it it always goes from Next number to the end of string 
hence for loop its ok to keep range from i to len(nums)
i=0, 
How many subordinates are required for us?
In permutation we need for the case of 123, for level=0 we need 3 subordinates
first subordinate returns all values with 1, 
second subordinate returns all values with 2, third one
with all values of 3.

For slot-1, we need only 2 subordoinatyes ..  so subordinates 
this is how subordinates are defined

In our case number of subordinates:
Take: ABC 
for first slot 
A, s-1-1: B calls s-2-1: and its printed 
Now B calls s-2-2: it prints BC 
Next A: s-1-2: B, B s-2-1: B, prints BB 

We need to print 
Then: BC
That means first subordinate always adds 
A 1-1: 2-1: B prints 
A 1-2: 2-1: C
What order we need to write: 
We have to write this program in include and exclude 
way. Becuase combinations are nothing but subsets of specified
length

Lets see the program:
if len(slate) == size_combination:
  result.add(slate)
  return

if i == n: # n is length of the input it simply returns 
  return 

helper(slate,i+1)
slate.append(nums[i],i+1)
helper(slate,i+1)
slate.pop()

Now let's debug data flow for: ABC, comb_size:2 

i am 0, my subordinate is 1 .. 
0->1:1 , ([],i=1), 
now 1->2:1 ([],i=2), here nothing is added to slate 
and since we are checking the condition len(slate) must be combination 
size it won't be added to slate.

Now 2->3:1 ([],i=3), since i==3 len then it returns 
now 2->3:2  adds C  to slate (C,i=3), since length of the slate not equal to 2, and 
since i=3 it returns 

Now 1->2:2 adds B (B,i=2) calls 2
2->3:1 adds nothing to slate and calls 3 (B,i=3), this returns back at second base condition 
After that 2->3:2 adds second element to slate (BC,i=3) calls 3, since length of 
the slate is 2 it is added to the result and comes back 
Similary all others are also happen







